import datetime
daysnum = 0
simeramera = datetime.datetime.now().day		
simeraminas = datetime.datetime.now().month 
simeraetos = datetime.datetime.now().year

datetime.datetime.today()
datetime.datetime(simeraetos, simeraminas, simeramera)
datetime.datetime.today().weekday()
dayoftheweek = datetime.datetime.today().weekday() 		#H deftera  einai to 0, h triti to 1, tetarti to 2 kai paei legontas, h kyriaki einai to 6

for y in range(0,10)
    for m in range(simeraminas+1,):
        if dayoftheweek == 
            daysnum = daysnum + 1
    simeraminas = 1
